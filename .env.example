# Base URL for the AgriFlow API (see backend/README.md). Must include the
# /api prefix.
VITE_API_URL=http://localhost:8080/api

# Production/staging: point at the live Railway deployment instead —
# VITE_API_URL=https://agriflow-api-production.up.railway.app/api

# Stellar Testnet Configuration
VITE_CONTRACT_ID=
VITE_USDC_CONTRACT=
STELLAR_SECRET=
